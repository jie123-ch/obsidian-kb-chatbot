# 知识库问答 · 多设备 / 多用户部署方案

> 背景：当前已有一个可运行的本地知识库聊天机器人（`kb-chatbot/`），在单机 localhost 跑通：
> 本机把 Obsidian vault 做向量化 → 提问时本机检索相关片段 → 只把「问题+片段」发给 DeepSeek 生成答案。
> 本文回答两个问题：(1) 换台电脑能否访问本机知识库；(2) 能否支撑 10 人以下多设备同时访问（小公司私有知识库）。

---

## 一、结论速览

| 问题 | 是否可行 | 说明 |
|---|---|---|
| 换台电脑访问本机知识库 | **可行** | 两条路径都可，推荐「vault 同步 + 新电脑跑服务」 |
| 10 人以下多设备同时访问 | **当前方案不可直接支撑，但核心逻辑可 100% 复用** | 需补齐：鉴权、并发、网络暴露、历史隔离 |

你的 RAG 核心（检索 / 嵌入 / prompt / 前端 UI）**完全不用重写**，只需在「部署层」做扩展。

---

## 二、问题 1：换台电脑访问本机知识库

知识库的「重」部分（vault 文件 + 向量索引 + 嵌入模型）都在原电脑上。要让另一台电脑能访问，本质是让那台电脑能连上服务能力。

### 方案 A：内网穿透 / 端口映射（零迁移）
- 用 frp / Cloudflare Tunnel / ngrok，把原电脑的 `localhost:8000` 暴露到公网或另一台电脑能达的地址。
- 优点：不动现有代码，原电脑上的知识库、索引、模型全部原地复用。
- 缺点：
  - **原电脑必须一直开机**；
  - 暴露到公网必须加鉴权（否则任何人都能消耗你的 DeepSeek key）；
  - 跨网络延迟依赖原电脑上行带宽。

### 方案 B：vault 同步 + 新电脑跑同一套服务（推荐）
- 用 Obsidian Sync / Git / 坚果云 / Syncthing 把 vault 同步到新电脑；
- 新电脑上跑同一套 `kb-chatbot`（重新 `build_index.py` 建索引即可）；
- 嵌入模型、向量库、前端全部在新电脑本地。
- 优点：不依赖原电脑开机、延迟低、隐私边界清晰（内容仍不出本机）。
- 缺点：要维护一份同步（vault 变动后重跑索引，可用定时任务自动触发）。

**建议**：若「换电脑」是偶尔在另一台自己的设备用 → 方案 B 最干净；若是要远程随时访问 → 方案 A（穿透）+ 单密码。

---

## 三、问题 2：10 人以下多设备同时访问（小公司私有知识库）

### 当前方案的瓶颈（为什么不能直接撑）

1. **无鉴权**：任何人拿到地址就能调用，直接消耗你的 DeepSeek key，且无审计。
2. **单进程 uvicorn**：嵌入模型（bge-small-zh）在 CPU 上推理，10 人并发查询会排队变慢。
3. **对话历史在浏览器 localStorage**：不按用户隔离（公用浏览器会串），换设备丢失，无法跨端续聊。
4. **仅本机可达**：无 HTTPS / 域名 / 反向代理，无法从外部网络安全访问。
5. **DeepSeek key 共享无限流**：免费版有 RPM/TPM 限制，一人刷问会拖垮全员。

### 可行性判断
- 单机的「检索+生成」逻辑**完全可复用**；
- 但「多用户并发访问」需要一套**常开的服务端 + 反向代理 + 鉴权 + 限流 + 后端历史**——这是当前缺失的部署层。

---

## 四、推荐目标架构（10 人私有知识库）

```
员工设备(手机/PC/平板) ×N
        │  HTTPS
        ▼
Nginx / Caddy 反向代理
  - 域名 + 自动 HTTPS(Let's Encrypt)
  - 基础鉴权 / 每用户账号(JWT)
  - 请求限流(防止刷爆 DeepSeek key)
        │
        ▼
应用服务器(一台常开机器)
  FastAPI + uvicorn(多 worker)
    ├─ bge-small-zh 嵌入模型常驻内存   (并发友好，CPU 即可)
    ├─ 向量库 Chroma / pgvector         (并发读，替代 SQLite)
    ├─ 对话历史 DB + user_id 隔离        (Postgres 或 SQLite)
    └─ 调用 DeepSeek 云端 BYOK + 限流
        ▲
        │ 同步
Obsidian vault ──(Git/Syncthing/坚果云)──► 服务器本地副本
```

### 关键决策点

| 决策 | 选项 | 推荐（10 人小团队） |
|---|---|---|
| 部署位置 | 云服务器 / 公司常开旧电脑或 NAS | 先旧电脑/NAS 零成本；要稳定可达再上云 |
| 鉴权强度 | 单密码共享 / 每用户账号 | 先单密码（Nginx 基础认证），需要审计再上每用户 |
| 向量库 | 保留 SQLite / 升 Chroma / pgvector | 保留 SQLite WAL 模式够用；要更稳选 Chroma |
| 嵌入模型 | 保留 bge-small-zh / 升级 | 保留即可，CPU 常驻，10 并发能扛 |
| 历史存储 | 浏览器 localStorage / 后端 DB | 迁后端 DB + user_id（支持换设备续聊） |
| 知识库同步 | 手动 / Git hook / Syncthing / 坚果云 | Syncthing 或 Git，变更后自动重建索引 |

---

## 五、实施路径（分阶段）

- **阶段 0 · 原型验证（约 1 天）**
  当前代码 + Cloudflare Tunnel 暴露 + 单密码，拉 3–5 人试用，验证体验与成本。

- **阶段 1 · 常开部署（约 2–3 天）**
  选一台常开机器，部署 FastAPI 多 worker + Caddy/Nginx（HTTPS + 域名），把当前 `app.py` / `retrieve.py` / 前端直接搬过去。

- **阶段 2 · 多用户加固（约 3–5 天）**
  加鉴权（账号/JWT）、对话历史后端化（按 user_id 隔离）、DeepSeek 调用限流、向量库并发优化。

- **阶段 3 · 自动化（持续）**
  Obsidian vault 自动同步到服务器；变更触发 `build_index.py` 增量/全量重建索引（定时任务或 webhook）。

---

## 六、成本估算（人民币 / 月）

| 项目 | 费用 |
|---|---|
| 云服务器（腾讯云轻量 2核4G） | 约 ¥60–100 / 月（可选，用旧电脑则 ¥0） |
| 域名 | ¥50–100 / 年（可选，IP + 自签也可） |
| HTTPS 证书 | 免费（Let's Encrypt / Caddy 自动签发） |
| 嵌入模型 | 本机跑，免费 |
| DeepSeek API | 按量，10 人小团队预计 ¥20–100 / 月 |
| **合计** | **约 ¥0–200 / 月** |

---

## 七、下一步建议

1. 先确认「换电脑」是自用还是远程访问，决定走方案 A 还是 B；
2. 若要做 10 人版，建议先用阶段 0（Tunnel + 单密码）快速验证，再投入阶段 1–2；
3. 需要我帮忙时，可以直接说「帮我做阶段 X」，我会基于现有 `kb-chatbot/` 代码扩展，而不是从头重写。
